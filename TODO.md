# TODO

- [ ] Update React Projects/“data” mode so clicking “Projects” shows a landing/choose-project state instead of immediately rendering a module table.
- [ ] Ensure the jobs module doesn’t show as the default active module when user enters Projects.
- [ ] Keep existing module-as-folder behavior: selecting a module still shows DataView editor/table.
- [ ] Run frontend build/dev sanity check (if applicable).

